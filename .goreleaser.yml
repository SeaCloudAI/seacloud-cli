version: 2

project_name: seacloud
dist: dist

builds:
  - id: seacloud
    main: .
    binary: seacloud
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - >-
        -s -w
        -X github.com/SeaCloudAI/seacloud-cli/internal/buildinfo.Version={{ .Version }}
        -X github.com/SeaCloudAI/seacloud-cli/internal/auth.BaseURL={{ .Env.SEACLOUD_BASE_URL }}
        -X github.com/SeaCloudAI/seacloud-cli/internal/models.BaseURL={{ .Env.SEACLOUD_MODELS_URL }}
        -X github.com/SeaCloudAI/seacloud-cli/internal/generation.BaseURL={{ .Env.SEACLOUD_GENERATION_URL }}
        -X github.com/SeaCloudAI/seacloud-cli/internal/skillhub.BaseURL={{ .Env.SEACLOUD_SKILLHUB_URL }}

archives:
  - id: default
    ids:
      - seacloud
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "SHA256SUMS"

release:
  github:
    owner: SeaCloudAI
    name: seacloud-cli

source:
  enabled: false

snapshot:
  version_template: "{{ .Version }}-next"

changelog:
  disable: true
